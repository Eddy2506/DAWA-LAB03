<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear Cliente</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body>
    <div class="container p-4" style="margin-top:70px ;">
        <div class="row">
            <div class="col-md-4 mx-auto">
                <div class="card text-center">
                    <div class="card-header">
                        <h3 class="text-uppercase">CREAR CLIENTE</h3>
                    </div>
                    <div class="card-body">
                        <form id="createClientForm" action="/add" method="post">
                            <div class="input-group mt-2">
                                <label for="name" class="input-group-text">Nombre</label>
                                <input class="form-control" type="text" name="nomcli" id="nomcli" placeholder="Nombres" autofocus required>
                            </div>
                            <div class="input-group mt-2">
                                <label for="lastname" class="input-group-text">Apellidos</label>
                                <input class="form-control" type="text" name="apecli" id="apecli" placeholder="Apellidos" required>
                            </div>
                            <div class="input-group mt-2">
                                <label for="age" class="input-group-text">Nro. DNI</label>
                                <input class="form-control" type="text" name="nrodnicli" id="nrodnicli" placeholder="Nro. DNI" required>
                            </div>
                            <div class="input-group mt-2">
                                <label for="age" class="input-group-text">Nro. Teléfono</label>
                                <input class="form-control" type="text" name="telcli" id="telcli" placeholder="Nro. Célular" required>
                            </div>
                            <div class="form-group mt-4 d-grid gap-2">
                                <button type="submit" class="btn btn-success"> Guardar</button>
                                <a class="btn btn-danger" href="/list"> Cancelar</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        document.querySelector('.btn-success').addEventListener('click', function(event) {
            event.preventDefault(); // Previene el comportamiento por defecto del botón

            Swal.fire({
                title: '¿Está seguro que desea guardar este cliente?',
                text: "Asegúrese de haber completado todos los campos correctamente.",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                confirmButtonText: 'Guardar',
                cancelButtonText: 'Cancelar'
            }).then((result) => {
                if (result.isConfirmed) {
                    document.getElementById('createClientForm').submit();
                }
            });
        });
    </script>
</body>
</html>
